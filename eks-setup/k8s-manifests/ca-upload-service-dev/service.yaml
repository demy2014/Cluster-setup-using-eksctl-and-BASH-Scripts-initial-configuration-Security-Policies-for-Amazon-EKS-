apiVersion: v1
kind: Service
metadata:
  name: ca-upload-service-dev-web
  namespace: default
spec:
  selector:
    app: ca-upload-service-dev-web
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer